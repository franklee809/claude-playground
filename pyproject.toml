[project]
name = "members-reader"
version = "0.1.0"
description = "Read and display member names from a CSV file"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "franklee809" }]
keywords = ["csv", "members", "reader"]
dependencies = []

[project.urls]
Homepage = "https://github.com/franklee809/claude-playground"
Repository = "https://github.com/franklee809/claude-playground"
Issues = "https://github.com/franklee809/claude-playground/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
